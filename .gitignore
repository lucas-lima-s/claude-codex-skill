# Local telemetry — machine-specific paths and timestamps
cache/
__pycache__/
*.py[cod]
*.pyo
*.egg-info/

# Editors
.vscode/
.idea/
*.swp

# System
.DS_Store
Thumbs.db

# Temporary outputs (in case scripts are run from inside the repo)
*.log
*.tmp

# Local configuration override (deep-merged over config.default.json).
# Each user can customize settings/locales without committing them.
config.local.json
